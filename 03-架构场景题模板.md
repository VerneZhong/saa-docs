# 03 - SAA 架构场景题模板

SAA 场景题通常不是问“哪个服务是什么”，而是问“在约束条件下，哪个架构最合适”。做题时先找目标词。

## 通用解题步骤

1. **看最后一句**：问的是成本、可用性、安全、性能、运维最少还是迁移？
2. **圈关键词**：multi-AZ、global、serverless、least operational overhead、cost-effective、near real-time。
3. **排除反模式**：硬编码密钥、单 AZ、手动运维、开放公网、自己造轮子。
4. **比较两个看似正确的选项**：选更托管、更符合约束、更少运维的。

---

## 模板 1：Web 应用高可用

### 题目关键词

- Web application
- highly available
- multiple AZs
- variable traffic
- minimal downtime

### 标准架构

- Route 53
- ALB
- Auto Scaling Group across multiple AZs
- EC2 in private subnets
- RDS Multi-AZ
- NAT Gateway for private outbound

### 判断规则

- Web HTTP/HTTPS → ALB。
- 流量变化 → ASG。
- 数据库高可用 → RDS Multi-AZ。
- EC2 不应直接放公网，除非明确需要。

---

## 模板 2：静态网站/图片下载全球加速

### 题目关键词

- static website
- images/videos
- global users
- low latency
- reduce origin load

### 标准架构

- S3 bucket as origin
- CloudFront distribution
- Origin Access Control
- Optional WAF

### 判断规则

- 全球访问低延迟 → CloudFront。
- 静态内容 → S3。
- 不希望 S3 公网直连 → OAC/OAI。

---

## 模板 3：私有子网访问 AWS 服务

### 题目关键词

- private subnet
- no internet access
- access S3/DynamoDB/AWS APIs
- secure private connectivity

### 标准架构

- S3/DynamoDB → Gateway VPC Endpoint
- 其他服务 → Interface VPC Endpoint

### 判断规则

- 如果题目强调“不经过公网”或“private connectivity”，优先 VPC Endpoint。
- NAT Gateway 能出网，但不是最私密/最安全答案。

---

## 模板 4：数据库高可用 vs 读扩展

### 题目关键词 A：高可用

- failover
- standby
- minimize downtime
- AZ failure

答案倾向：RDS Multi-AZ / Aurora。

### 题目关键词 B：读扩展

- read-heavy
- offload read traffic
- reporting queries

答案倾向：Read Replica / Aurora Replica。

### 记忆

- 传统 Multi-AZ DB instance = 保命，单 standby 不承担读流量。
- Multi-AZ DB cluster = 保命 + 两个可读 standby。
- Read Replica = 分担读。

---

## 模板 5：异步解耦削峰

### 题目关键词

- traffic spikes
- decouple components
- messages processed later
- prevent data loss
- workers process jobs

### 标准架构

- Producer → SQS → Consumer workers / Lambda
- DLQ for failed messages
- ASG scale based on queue depth

### 判断规则

- 削峰填谷 → SQS。
- 需要严格顺序 → SQS FIFO。
- 失败隔离 → DLQ。

---

## 模板 6：发布订阅/广播

### 题目关键词

- notify multiple systems
- fanout
- email/SMS/HTTP subscribers
- one event to many consumers

### 标准架构

- SNS topic
- Multiple subscriptions
- Optional SQS queues per consumer

### 判断规则

- 一发多收 → SNS。
- 每个消费者独立缓冲 → SNS + SQS。

---

## 模板 7：事件驱动与 SaaS/AWS 服务事件

### 题目关键词

- event pattern
- route events
- SaaS integration
- scheduled rule
- AWS service event

### 标准架构

- EventBridge rule/bus → target Lambda/SQS/SNS/Step Functions

### 判断规则

- 事件规则路由 → EventBridge。
- 定时触发 → EventBridge Scheduler/Rule。

---

## 模板 8：Serverless API

### 题目关键词

- REST API
- no server management
- unpredictable traffic
- scale automatically

### 标准架构

- API Gateway
- Lambda
- DynamoDB / Aurora Serverless
- Cognito if user auth needed

### 判断规则

- “least operational overhead” 强烈暗示 Serverless/托管服务。

---

## 模板 9：跨区域灾备

### 题目关键词

- disaster recovery
- RTO / RPO
- another region
- business continuity

### 解法层级

| 策略 | 成本 | 恢复速度 |
|---|---|---|
| Backup and Restore | 最低 | 慢 |
| Pilot Light | 低 | 中 |
| Warm Standby | 中 | 快 |
| Multi-site Active/Active | 高 | 最快 |

### 判断规则

- 成本最低 → Backup and Restore。
- 快速恢复但控制成本 → Pilot Light / Warm Standby。
- 几乎无中断 → Active/Active。

---

## 模板 10：本地到 AWS 连接

### 题目关键词

- on-premises data center
- hybrid cloud
- consistent low latency
- private connection
- encrypted connection over internet

### 标准判断

- 快速搭建加密连接 → Site-to-Site VPN。
- 稳定低延迟专线 → Direct Connect。
- Direct Connect 加密 → DX + VPN。
- 多 VPC 混合连接中心 → Transit Gateway。

---

## 模板 11：大规模数据迁移

### 题目关键词

- petabytes
- limited bandwidth
- physical device
- offline migration

答案：Snow Family（常见为 Snowball Edge）。具体可用设备和区域以题目给出的选项为准，不把已变化的产品选项当固定口诀。

### 在线迁移

- 文件同步 → DataSync。
- 数据库迁移 → DMS。
- 本地应用持续访问云存储 → Storage Gateway。

---

## 模板 12：安全密钥与凭证

### 题目关键词

- encrypt data at rest
- manage keys
- rotate database credentials
- avoid hardcoded credentials

### 判断规则

- 加密密钥 → KMS。
- 密码/API Key 且要轮换 → Secrets Manager。
- EC2/Lambda 访问 AWS → IAM Role。
- 配置参数 → SSM Parameter Store。

---

## 模板 13：监控审计合规

### 题目关键词

- metric / alarm / logs → CloudWatch
- who made API call → CloudTrail
- resource configuration changed → AWS Config
- compliance rule → AWS Config

### 典型答案

- CPU 超阈值通知 → CloudWatch Alarm + SNS。
- 查谁删除了资源 → CloudTrail。
- 检查 S3 是否公开 → AWS Config rule。

---

## 模板 14：成本优化

### 题目关键词

- cost-effective
- infrequently accessed
- steady-state workload
- interruptible workload
- reduce storage cost

### 判断规则

- 冷数据 → S3 Lifecycle 到 Glacier。
- 访问模式不明 → Intelligent-Tiering。
- 稳定 EC2 → Reserved / Savings Plans。
- 可中断计算 → Spot。
- 托管/Serverless 有时比自建更省运维成本。

---

## 模板 15：性能优化

### 题目关键词

- low latency global users → CloudFront / Global Accelerator
- database read latency → Read Replica / ElastiCache
- session store → ElastiCache Redis / DynamoDB
- high IOPS block storage → EBS io2/gp3

### CloudFront vs Global Accelerator

- HTTP 静态/动态内容缓存 → CloudFront。
- TCP/UDP 应用全球入口、固定 Anycast IP → Global Accelerator。

---

## 最常见陷阱

1. **题目问高可用，却选 Read Replica**：错，通常要 Multi-AZ。
2. **题目问读扩展，看到 Multi-AZ 就一律排除**：不严谨。传统 Multi-AZ DB instance 的单 standby 不可读；Multi-AZ DB cluster 的两个 standby 可读。若题目只给传统 Multi-AZ 与 Read Replica，读扩展通常选 Read Replica。
3. **私有访问 AWS 服务却选 NAT Gateway**：如果强调安全私网，选 VPC Endpoint。
4. **问最少运维却选自建 EC2 集群**：通常错。
5. **问一发多收却选 SQS**：广播应选 SNS，缓冲才是 SQS。
6. **问事件规则路由却选 SNS**：复杂事件匹配一般 EventBridge。
7. **问审计 API 调用却选 CloudWatch**：应该 CloudTrail。
