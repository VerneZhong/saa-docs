# 05 - AWS SAA 中文模拟题 30 道（带答案解析）

说明：这些题是训练 SAA-C03 常见架构判断，不是官方原题。做题时请先遮住答案，做完再看解析。

---

## 1. 私有子网访问 S3

公司有 EC2 实例运行在私有子网中，需要访问 S3 下载文件。安全团队要求流量不要经过公网，并尽量降低成本。应选择什么？

A. NAT Gateway  
B. Internet Gateway  
C. Gateway VPC Endpoint for S3  
D. Site-to-Site VPN

**答案：C**

**解析：** 私有访问 S3 且不经过公网，选 S3 Gateway VPC Endpoint。NAT Gateway 可以出公网，但不符合“不经过公网”和最低成本。

---

## 2. RDS 高可用

一个生产 MySQL 数据库运行在 Amazon RDS 上，公司要求在单个 AZ 故障时自动故障切换，并尽量减少停机。应使用什么？

A. RDS Read Replica  
B. RDS Multi-AZ  
C. S3 Replication  
D. ElastiCache

**答案：B**

**解析：** RDS Multi-AZ 提供同步 standby 和自动故障切换。Read Replica 用于读扩展，不是高可用首选。

---

## 3. RDS 读扩展

一个电商系统数据库读请求很多，写请求较少。需要降低主数据库读压力。应选择什么？

A. RDS Multi-AZ  
B. RDS Read Replica  
C. AWS Backup  
D. NAT Gateway

**答案：B**

**解析：** 读多场景用 Read Replica。Multi-AZ 是故障切换，不用于分担读请求。

---

## 4. 全球静态内容加速

用户分布在全球，需要低延迟访问图片和 CSS/JS 文件。源文件存储在 S3。最佳方案是什么？

A. S3 + CloudFront  
B. S3 + NAT Gateway  
C. EBS + ALB  
D. RDS + Route 53

**答案：A**

**解析：** 静态内容全球分发用 CloudFront，源站可用 S3。

---

## 5. HTTP 路径路由

公司有多个微服务，要求根据 URL path `/api`、`/admin` 路由到不同目标组。应使用哪种负载均衡器？

A. NLB  
B. ALB  
C. GWLB  
D. Classic Load Balancer

**答案：B**

**解析：** ALB 是七层负载均衡，支持路径和主机名路由。

---

## 6. TCP 高性能与固定 IP

一个游戏服务使用 TCP，需要超低延迟、高吞吐，并希望负载均衡器提供静态 IP。应使用什么？

A. ALB  
B. NLB  
C. CloudFront  
D. API Gateway

**答案：B**

**解析：** NLB 适合 L4 TCP/UDP/TLS、高性能和静态 IP。

---

## 7. 消息削峰

订单系统在促销时流量突增，后端处理服务容易被打爆。要求保存请求并让 worker 按能力处理。应使用什么？

A. SNS  
B. SQS  
C. CloudTrail  
D. AWS Config

**答案：B**

**解析：** SQS 用于队列、削峰和解耦。

---

## 8. 一条消息通知多个系统

上传图片后，需要同时通知缩略图服务、审核服务和日志服务。最佳解耦方式是什么？

A. 单个 SQS 队列  
B. SNS topic，多个订阅者  
C. RDS Trigger  
D. NAT Gateway

**答案：B**

**解析：** 一发多收是 SNS。若每个系统还需要缓冲，可 SNS fanout 到多个 SQS。

---

## 9. 事件规则路由

公司希望根据不同 AWS 服务事件匹配规则，并路由到 Lambda 或 Step Functions。应使用什么？

A. EventBridge  
B. EBS  
C. IAM Access Analyzer  
D. CloudFront

**答案：A**

**解析：** EventBridge 是事件总线，适合事件匹配和路由。

---

## 10. 多步骤工作流

一个订单处理流程包括付款、库存检查、发货、失败重试和人工审核状态。希望最少运维。应使用什么？

A. Step Functions  
B. EC2 cron 脚本  
C. S3 Lifecycle  
D. VPC Peering

**答案：A**

**解析：** 多步骤、有状态、重试和分支的流程适合 Step Functions。

---

## 11. API 审计

安全团队想知道是谁在昨天删除了某个 S3 bucket。应查看什么？

A. CloudWatch Metrics  
B. CloudTrail  
C. AWS Config only  
D. Trusted Advisor

**答案：B**

**解析：** CloudTrail 记录 AWS API 调用，可审计谁做了什么。

---

## 12. 指标告警

需要在 EC2 CPU 超过 80% 持续 5 分钟时通知运维。应使用什么？

A. CloudTrail + SQS  
B. CloudWatch Alarm + SNS  
C. AWS Config + EFS  
D. S3 Event Notification only

**答案：B**

**解析：** 指标告警用 CloudWatch Alarm，通知可用 SNS。

---

## 13. 配置合规

公司要求检测所有 S3 bucket 是否禁止公共读。哪个服务最合适？

A. AWS Config  
B. CloudTrail  
C. ElastiCache  
D. DMS

**答案：A**

**解析：** AWS Config 用于记录资源配置并根据规则检查合规。

---

## 14. 数据库密码轮换

应用需要安全保存 RDS 密码，并自动轮换。应使用什么？

A. KMS  
B. Secrets Manager  
C. S3 Versioning  
D. IAM User Access Key

**答案：B**

**解析：** Secrets Manager 适合保存密钥/密码并支持自动轮换。KMS 管理加密密钥，不直接负责密码轮换。

---

## 15. EC2 访问 S3 的凭证

EC2 上的应用需要访问 S3。最佳安全实践是什么？

A. 把 Access Key 写进代码  
B. 使用 IAM Role 附加到 EC2  
C. 使用 root 用户密钥  
D. 把密钥存在 AMI 中

**答案：B**

**解析：** AWS 服务访问应使用 IAM Role，避免硬编码长期凭证。

---

## 16. 多 Linux EC2 共享文件

多个 AZ 中的 Linux EC2 需要共享同一套文件，并支持并发访问。应使用什么？

A. EBS  
B. EFS  
C. Instance Store  
D. S3 Glacier Deep Archive

**答案：B**

**解析：** EFS 是多 AZ 共享 NFS 文件系统，适合多个 Linux EC2 共享。

---

## 17. Windows 文件共享

企业需要迁移 Windows 文件共享到 AWS，并继续使用 SMB 协议。应使用什么？

A. FSx for Windows File Server  
B. EFS  
C. S3 Standard-IA  
D. DynamoDB

**答案：A**

**解析：** Windows SMB 文件共享适合 FSx for Windows File Server。

---

## 18. S3 访问模式不确定

公司有大量对象，访问频率无法预测，希望自动优化存储成本且不影响访问。应使用什么？

A. S3 Standard  
B. S3 Intelligent-Tiering  
C. S3 Glacier Deep Archive only  
D. EBS gp3

**答案：B**

**解析：** 访问模式不确定时，S3 Intelligent-Tiering 可自动在访问层之间移动。

---

## 19. 冷数据最低成本归档

合规数据需要保留 7 年，几乎不会访问，可以接受数小时恢复时间。应使用什么？

A. S3 Standard  
B. S3 One Zone-IA  
C. S3 Glacier Deep Archive  
D. EFS

**答案：C**

**解析：** Glacier Deep Archive 是长期归档最低成本选项，恢复时间较长。

---

## 20. DynamoDB 全球多区域写入

一个全球应用需要用户在多个区域都能低延迟读写同一张 NoSQL 表。应使用什么？

A. DynamoDB Global Tables  
B. RDS Multi-AZ  
C. S3 CRR  
D. ElastiCache Memcached

**答案：A**

**解析：** DynamoDB Global Tables 支持多区域多活复制和本地低延迟访问。

---

## 21. DynamoDB 读缓存

应用频繁读取 DynamoDB 中相同数据，希望降低读取延迟。应使用什么？

A. DAX  
B. RDS Proxy  
C. NAT Gateway  
D. AWS Config

**答案：A**

**解析：** DAX 是 DynamoDB Accelerator，用于 DynamoDB 缓存。

---

## 22. 多 VPC 大规模互联

公司有 50 个 VPC，需要集中互联并连接本地数据中心，要求架构易管理。应使用什么？

A. 大量 VPC Peering 全互联  
B. Transit Gateway  
C. 单个 NAT Gateway  
D. CloudFront

**答案：B**

**解析：** 多 VPC 大规模互联适合 Transit Gateway。VPC Peering 不支持传递路由，规模大时复杂。

---

## 23. 本地到 AWS 稳定专线

公司本地数据中心需要到 AWS 的稳定、低延迟、专用网络连接。应选择什么？

A. Direct Connect  
B. Internet Gateway  
C. CloudFront  
D. SQS

**答案：A**

**解析：** Direct Connect 提供专用网络连接，适合稳定低延迟混合云。

---

## 24. 快速建立加密混合连接

公司希望尽快让本地数据中心通过加密连接访问 VPC，成本低、上线快。应选择什么？

A. Site-to-Site VPN  
B. Direct Connect only  
C. S3 Transfer Acceleration  
D. EFS

**答案：A**

**解析：** Site-to-Site VPN 通过公网建立加密隧道，上线快。Direct Connect 更稳定但开通较慢。

---

## 25. 数据库迁移

公司要把本地 MySQL 迁移到 Amazon Aurora，并希望迁移期间源数据库尽量不中断。应使用什么？

A. AWS DMS  
B. Snowball only  
C. CloudTrail  
D. EBS Snapshot

**答案：A**

**解析：** Database Migration Service 支持数据库迁移，并可进行持续复制减少停机。

---

## 26. 在线文件同步

公司需要把本地 NAS 中的大量文件持续同步到 S3。应使用什么？

A. AWS DataSync  
B. AWS DMS  
C. RDS Read Replica  
D. API Gateway

**答案：A**

**解析：** DataSync 适合本地文件系统与 AWS 存储之间的数据传输。

---

## 27. 海量离线迁移

公司要迁移 500TB 数据到 AWS，但网络带宽很小，要求成本可控。应使用什么？

A. Snowball  
B. NAT Gateway  
C. CloudWatch Logs  
D. RDS Snapshot

**答案：A**

**解析：** 大规模离线迁移且带宽有限，使用 Snowball 设备。

---

## 28. 成本优化：稳定 EC2 负载

一组 EC2 实例会持续运行 3 年，利用率稳定。希望降低成本。应选择什么？

A. On-Demand only  
B. Reserved Instances 或 Savings Plans  
C. Spot only  
D. Dedicated Host always

**答案：B**

**解析：** 长期稳定负载适合 Reserved Instances 或 Savings Plans。

---

## 29. 成本优化：可中断批处理

公司每天运行批处理任务，任务可中断并能重试。希望最低计算成本。应使用什么？

A. Spot Instances  
B. Dedicated Hosts  
C. RDS Multi-AZ  
D. AWS Shield Advanced

**答案：A**

**解析：** 可中断、可重试任务适合 Spot，成本最低。

---

## 30. 最少运维的无服务器 API

创业团队要快速上线 REST API，流量不可预测，不想管理服务器。数据为简单键值访问。应选择什么？

A. EC2 + 自建数据库  
B. API Gateway + Lambda + DynamoDB  
C. ALB + 单台 EC2 + EBS  
D. Direct Connect + RDS Custom

**答案：B**

**解析：** Serverless API 场景下，API Gateway + Lambda + DynamoDB 运维最少、自动扩展，符合不可预测流量。

---

# 答案速查

1 C，2 B，3 B，4 A，5 B，6 B，7 B，8 B，9 A，10 A，  
11 B，12 B，13 A，14 B，15 B，16 B，17 A，18 B，19 C，20 A，  
21 A，22 B，23 A，24 A，25 A，26 A，27 A，28 B，29 A，30 B。
