# 07 - SAA 服务选择决策树

这份文档按“题干最后一句问什么”来选服务。做题时先判断目标：安全、可靠、性能、成本、运维最少、迁移。

## 0. 通用入口

1. 问“最少运维 / no server management” → 先想 Serverless / 托管服务：Lambda、API Gateway、DynamoDB、Aurora Serverless、Fargate、S3、Athena。
2. 问“高可用 / minimize downtime” → 多 AZ、托管 HA、自动故障切换、解耦。
3. 问“低成本 / cost-effective” → 生命周期、Spot、Savings Plans、Serverless、按访问模式选存储类。
4. 问“安全 / private / encrypted / audit” → IAM Role、KMS、VPC Endpoint、CloudTrail、Config、WAF。
5. 问“低延迟 / high performance” → CloudFront/GA、缓存、读副本、合适存储和数据库。

---

## 1. 访问控制与加密怎么选？

### 应用需要访问 AWS 服务

- EC2/Lambda/ECS 访问 S3/DynamoDB/RDS 等 → **IAM Role**。
- 跨账号访问 → **AssumeRole**，第三方账号加 **External ID**。
- 人员统一登录多个 AWS 账号 → **IAM Identity Center**。
- 组织级禁止某操作 → **Organizations SCP**。

### 数据需要加密

- 只要求默认服务端加密，运维最少 → 服务默认加密 / SSE-S3。
- 需要客户管理密钥、审计、跨账号控制 → **KMS customer managed key**。
- 需要自动轮换数据库密码 → **Secrets Manager**。
- 配置参数集中保存且低成本 → **SSM Parameter Store**。
- 必须自己管理硬件密钥模块 → **CloudHSM**。

### 需要安全检测

- 谁调用了 API？→ **CloudTrail**。
- 资源配置是否合规？→ **AWS Config**。
- 可疑账号行为/恶意 IP/威胁检测？→ **GuardDuty**。
- Web 攻击 SQLi/XSS？→ **WAF**。
- S3 敏感数据发现？→ **Macie**。
- EC2/ECR/Lambda 漏洞？→ **Inspector**。

---

## 2. 网络怎么选？

### 私有子网要出网或访问 AWS 服务

- 私有子网访问互联网下载补丁 → **NAT Gateway**。
- 私有访问 S3/DynamoDB，不走公网且省钱 → **Gateway VPC Endpoint**。
- 私有访问其他 AWS API/自建服务 → **Interface Endpoint / PrivateLink**。
- IPv6 私有子网只出不进 → **Egress-only Internet Gateway**。

### VPC 之间怎么连？

- 两个 VPC 简单互联，无重叠 CIDR → **VPC Peering**。
- 很多 VPC + 本地网络，集中路由 → **Transit Gateway**。
- 提供服务给其他 VPC/账号，消费者不进你 VPC → **PrivateLink**。
- 跨账号共享子网/TGW/Resolver 规则 → **AWS RAM**。

### 本地到 AWS 怎么连？

- 快速、低成本、加密公网隧道 → **Site-to-Site VPN**。
- 稳定低延迟、大带宽、专用链路 → **Direct Connect**。
- Direct Connect 还要加密 → **DX + VPN**。
- 远程员工访问 VPC → **Client VPN**。
- 混合 DNS 查询 → **Route 53 Resolver inbound/outbound endpoint**。

### 全球入口怎么选？

- HTTP/HTTPS 内容，能缓存 → **CloudFront**。
- TCP/UDP、不能缓存、需要 Anycast 静态 IP → **Global Accelerator**。
- DNS 层按延迟/权重/故障切换 → **Route 53 routing policy**。

---

## 3. 负载均衡怎么选？

- HTTP/HTTPS、路径路由、主机路由、容器目标 → **ALB**。
- TCP/UDP/TLS、超低延迟、固定 IP → **NLB**。
- 第三方防火墙/入侵检测设备插入流量路径 → **GWLB**。
- 全球 HTTP 边缘缓存 → **CloudFront**，不是 ELB。

常见组合：Route 53 → CloudFront/WAF → ALB → ASG/ECS → RDS/Aurora。

---

## 4. 计算怎么选？

### 服务器/容器/函数

- 需要完全控制 OS、安装代理、传统应用 → **EC2**。
- 需要自动扩缩 EC2 → **Auto Scaling Group**。
- Docker 工作负载，AWS 原生、少运维 → **ECS**。
- Docker 工作负载，不想管节点 → **ECS Fargate**。
- 必须 Kubernetes 生态/API → **EKS**。
- 短任务、事件驱动、自动扩展 → **Lambda**。
- REST/HTTP/WebSocket API 无服务器入口 → **API Gateway**。

### EC2 购买选项

- 短期、不可中断、需求不确定 → **On-Demand**。
- 1/3 年稳定用量，想灵活覆盖 EC2/Fargate/Lambda → **Savings Plans**。
- 特定实例长期稳定 → **Reserved Instances**。
- 可中断、可重试、批处理/worker → **Spot**。
- 必须拿到某 AZ 容量 → **Capacity Reservation**。
- BYOL/物理主机合规 → **Dedicated Host**。

---

## 5. 存储怎么选？

### 对象/块/文件

- 海量对象、静态网站、数据湖、备份 → **S3**。
- 单台 EC2 的块设备 → **EBS**。
- 临时高速本地盘，可丢数据 → **Instance Store**。
- 多 AZ Linux 共享文件 → **EFS**。
- Windows SMB 文件共享 → **FSx for Windows File Server**。
- HPC/ML 高吞吐文件系统，可接 S3 → **FSx for Lustre**。
- 本地应用继续用 NFS/SMB/iSCSI/磁带接口访问云存储 → **Storage Gateway**。

### S3 存储类

- 频繁访问 → Standard。
- 低频但要毫秒访问 → Standard-IA。
- 可重建、单 AZ 低频 → One Zone-IA。
- 不知道访问模式 → Intelligent-Tiering。
- 归档但偶尔毫秒访问 → Glacier Instant Retrieval。
- 分钟到小时恢复 → Glacier Flexible Retrieval。
- 最低成本，小时级恢复 → Deep Archive。

### S3 功能

- 防误删/保留版本 → Versioning。
- 自动降级/删除 → Lifecycle。
- WORM 合规 → Object Lock。
- 跨区域复制 → CRR。
- 临时下载链接 → Presigned URL。
- CloudFront 私有访问 S3 → OAC。

---

## 6. 数据库怎么选？

### 关系型

- MySQL/PostgreSQL/SQL Server/Oracle 托管 → **RDS**。
- AWS 云原生高性能 MySQL/PostgreSQL 兼容 → **Aurora**。
- 高可用自动故障切换 → **Multi-AZ**。
- 读扩展/报表查询 → **Read Replica / Aurora Replica**。
- 跨区域低 RPO 灾备 → **Aurora Global Database**。
- Lambda 大量连接关系库 → **RDS Proxy**。
- 需要 OS/数据库底层更高控制 → **RDS Custom**。

### NoSQL / 缓存 / 图 / 文档

- Key-value/document、毫秒级、Serverless → **DynamoDB**。
- DynamoDB 全球多区域多活 → **Global Tables**。
- DynamoDB 读缓存 → **DAX**。
- 会话缓存、排行榜、复杂缓存 → **ElastiCache Redis/Valkey**。
- 简单分布式缓存 → **Memcached**。
- 图关系查询 → **Neptune**（SAA 低频）。
- MongoDB 兼容文档库 → **DocumentDB**（SAA 低频）。

### 分析/搜索

- S3 上直接 SQL、偶发查询 → **Athena**。
- ETL/Data Catalog/Crawler → **Glue**。
- 数据仓库 OLAP → **Redshift**。
- 日志全文搜索/可视化 → **OpenSearch**。
- 实时流摄取/处理 → **Kinesis Data Streams**。
- 托管投递到 S3/Redshift/OpenSearch → **Kinesis Data Firehose**。

---

## 7. 解耦与工作流怎么选？

- 削峰、队列、worker 拉取 → **SQS**。
- 严格顺序/去重 → **SQS FIFO**。
- 一条消息推给多个订阅者 → **SNS**。
- 广播且每个消费者要独立缓冲 → **SNS + SQS fanout**。
- 事件规则匹配、SaaS/AWS 服务事件、定时 → **EventBridge**。
- 多步骤、有状态、重试、分支、人工审核 → **Step Functions**。
- API 同步调用 → **API Gateway/ALB**，不要把 SQS 当同步响应。

---

## 8. 迁移怎么选？

- 数据库迁移，低停机，CDC → **DMS**。
- 异构数据库 schema 转换 → **SCT**。
- 服务器 lift-and-shift → **Application Migration Service**。
- 在线文件同步到 S3/EFS/FSx → **DataSync**。
- TB/PB，网络太慢 → **Snowball Edge**。
- 本地应用持续以文件/块/磁带方式使用云 → **Storage Gateway**。
- SFTP/FTPS/FTP 托管入口 → **Transfer Family**。

---

## 9. 灾备怎么选？

- 成本最低、能接受较长恢复 → **Backup and Restore**。
- 只运行核心数据库/复制，灾难时启动计算 → **Pilot Light**。
- 缩小版全栈一直运行，灾难时扩容 → **Warm Standby**。
- 两地同时承载流量，RTO/RPO 最低 → **Multi-site Active/Active**。

常见服务：AWS Backup、EBS/RDS snapshots、S3 CRR、DynamoDB Global Tables、Aurora Global Database、Route 53 Failover、Global Accelerator。

---

## 10. 成本优化怎么选？

- EC2 长期稳定 → Savings Plans/RI。
- 可中断 → Spot。
- S3 冷数据 → Lifecycle 到 Glacier。
- 访问模式未知 → Intelligent-Tiering。
- 数据库间歇/波动 → Aurora Serverless / DynamoDB On-demand。
- 查询 S3 数据偶发 → Athena，不先建 Redshift。
- 全球静态内容反复下载 → CloudFront 缓存降低源站与传输成本。
- 不需要自管时 → 托管/Serverless 可降低运维成本。
