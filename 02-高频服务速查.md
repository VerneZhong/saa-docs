# 02 - SAA 高频服务速查

## 一句话判断表

| 场景 | 优先服务 |
|---|---|
| 静态网站托管 | S3 + CloudFront |
| 全球低延迟内容分发 | CloudFront |
| HTTP/HTTPS 路由 | ALB |
| TCP/UDP 超高性能负载均衡 | NLB |
| EC2 自动扩缩容 | Auto Scaling Group |
| 私有子网访问互联网 | NAT Gateway |
| 私网访问 S3/DynamoDB | Gateway VPC Endpoint |
| 私网访问 AWS API | Interface VPC Endpoint / PrivateLink |
| 多 VPC 大规模互联 | Transit Gateway |
| 关系型数据库高可用 | RDS Multi-AZ |
| 关系型数据库读扩展 | Read Replica |
| 全球低延迟关系型数据库 | Aurora Global Database |
| NoSQL 高并发键值访问 | DynamoDB |
| DynamoDB 全球多区域多活 | Global Tables |
| 数据库/会话缓存 | ElastiCache |
| 消息削峰解耦 | SQS |
| 消息广播 | SNS |
| 事件路由 | EventBridge |
| 多步骤工作流 | Step Functions |
| 无服务器 API | API Gateway + Lambda |
| API 调用审计 | CloudTrail |
| 指标日志告警 | CloudWatch |
| 资源合规检查 | AWS Config |
| 密钥管理 | KMS |
| 密码自动轮换 | Secrets Manager |
| S3 上 SQL 查询 | Athena |
| 数据仓库 | Redshift |
| 迁移数据库 | DMS |
| 在线文件传输 | DataSync |
| 混合云文件网关 | Storage Gateway |
| 海量离线迁移 | Snowball |

---

## 计算服务

### EC2

适合：

- 需要控制 OS、网络、安装软件。
- 传统应用迁移。
- 长时间运行的服务器。

购买选项：

- **On-Demand**：灵活，无承诺，成本较高。
- **Reserved Instances / Savings Plans**：长期稳定负载省钱。
- **Spot**：极低成本，可被回收，适合可中断任务。
- **Dedicated Host**：许可绑定、合规隔离。

### Auto Scaling Group

关键词：

- 自动扩缩容
- 跨 AZ 部署
- 根据 CPU/请求数扩容
- 替换不健康实例

### Lambda

适合：

- 事件驱动。
- 短任务。
- 运维最少。
- 和 S3、DynamoDB、SQS、EventBridge 集成。

注意：

- 有超时限制。
- 冷启动可能影响延迟。
- 不适合长时间运行任务。

---

## 负载均衡

### ALB vs NLB vs GWLB

| 服务 | 层级 | 典型场景 |
|---|---|---|
| ALB | L7 | HTTP/HTTPS、路径/主机路由、容器服务 |
| NLB | L4 | TCP/UDP/TLS、极高性能、固定 IP、低延迟 |
| GWLB | L3/L4 | 部署第三方安全设备、防火墙流量检查 |

---

## 存储服务

### S3 Storage Class

| 类型 | 场景 |
|---|---|
| S3 Standard | 高频访问 |
| S3 Standard-IA | 低频访问，仍需快速取回 |
| S3 One Zone-IA | 可重建、低频、单 AZ 成本低 |
| S3 Intelligent-Tiering | 访问模式不确定 |
| Glacier Instant Retrieval | 归档但需要毫秒访问 |
| Glacier Flexible Retrieval | 分钟到小时级归档 |
| Glacier Deep Archive | 最低成本，小时级恢复 |

S3 高频功能：

- Versioning：版本保护。
- Lifecycle：自动转存储层/删除。
- Replication：跨区域/同区域复制。
- Object Lock：WORM，防篡改。
- Presigned URL：临时访问。

### EBS vs EFS vs S3 vs FSx

| 服务 | 类型 | 场景 |
|---|---|---|
| EBS | 块存储 | 单 AZ EC2 磁盘 |
| EFS | 文件存储 | 多 AZ 多 EC2 Linux 共享 |
| S3 | 对象存储 | 海量对象、静态资源、数据湖 |
| FSx | 托管文件系统 | Windows / Lustre / NetApp / OpenZFS |

---

## 数据库服务

### RDS

关键词：

- MySQL/PostgreSQL/MariaDB/Oracle/SQL Server。
- Multi-AZ 用于高可用。
- Read Replica 用于读扩展。
- Automated Backup 支持 PITR。

### Aurora

关键词：

- AWS 云原生关系数据库。
- 读副本多、性能好。
- Aurora Serverless 适合间歇/不稳定负载。
- Aurora Global Database 适合跨区域低 RPO 灾备。

### DynamoDB

关键词：

- Serverless NoSQL。
- 单位数毫秒延迟。
- On-demand 适合不可预测流量。
- Global Tables 适合全球多区域写入。
- DAX 是缓存。

---

## 网络服务

### NAT Gateway vs Internet Gateway

- Internet Gateway：让公有子网资源能与互联网通信。
- NAT Gateway：让私有子网主动访问互联网，但互联网不能主动访问私有实例。

### Security Group vs NACL

| 项目 | Security Group | NACL |
|---|---|---|
| 层级 | ENI/实例 | 子网 |
| 状态 | Stateful | Stateless |
| 规则 | Allow only | Allow + Deny |
| 高频 | 实例防火墙 | 子网边界控制 |

### VPC Endpoint

- Gateway Endpoint：S3、DynamoDB。
- Interface Endpoint：大多数 AWS 服务，通过 ENI/PrivateLink。

---

## 解耦与事件

### SQS vs SNS vs EventBridge

| 服务 | 核心用途 | 记忆法 |
|---|---|---|
| SQS | 队列、削峰、消费者拉取 | 排队等处理 |
| SNS | 发布订阅、广播推送 | 一发多收 |
| EventBridge | 事件总线、规则路由、SaaS 集成 | 事件规则中心 |

### Standard vs FIFO SQS

- Standard：最高吞吐，至少一次，可能乱序。
- FIFO：严格顺序，Exactly-once processing 语义，吞吐相对低。

---

## 安全与监控

### IAM

高频规则：

- 应用访问 AWS 服务用 IAM Role。
- 不要硬编码 Access Key。
- 最小权限。
- 跨账号用 AssumeRole。

### KMS / Secrets Manager / Parameter Store

| 服务 | 场景 |
|---|---|
| KMS | 加密密钥管理 |
| Secrets Manager | 数据库密码/API 密钥，自动轮换 |
| SSM Parameter Store | 配置参数、简单密钥存储 |

### CloudWatch / CloudTrail / Config

| 服务 | 场景 |
|---|---|
| CloudWatch | 指标、日志、告警 |
| CloudTrail | 记录 API 调用，审计谁做了什么 |
| AWS Config | 记录资源配置变化，合规规则 |

---

## 迁移与混合云

| 场景 | 服务 |
|---|---|
| 数据库迁移 | DMS |
| 服务器迁移 | Application Migration Service |
| 在线文件同步 | DataSync |
| 本地访问云存储 | Storage Gateway |
| 专线连接 | Direct Connect |
| 加密公网隧道 | Site-to-Site VPN |
| 大量离线数据迁移 | Snowball |

---

## 高频易混点

- **RDS Multi-AZ**：高可用，不用于读扩展。
- **RDS Read Replica**：读扩展，可跨区域，但不是自动故障切换的首选。
- **S3 CRR**：跨区域复制，不等于备份全部历史，建议配合 Versioning。
- **CloudFront**：缓存和全球加速，不是源站本身。
- **Route 53 Failover**：DNS 故障切换，不等于应用层自动修复。
- **NAT Gateway**：私有子网出网，不允许互联网主动访问私有实例。
- **VPC Peering**：不支持传递路由；多 VPC 用 Transit Gateway。
