# 10 - 日语面试表达：AWS 架构

这份给在日本求职/面试时使用。目标不是背复杂日语，而是能用 N2 左右表达清楚“我为什么这样设计”。每段都可以改成自己的项目经历。

## 基础自我表达

### 中文思路

我学习 AWS SAA 时，重点理解了高可用、安全、性能和成本优化。设计架构时，我会先确认业务要求，例如 RTO/RPO、访问量、数据敏感性和预算，然后选择托管服务减少运维成本。

### 日本語

AWS SAA の学習では、高可用性、セキュリティ、パフォーマンス、コスト最適化を重点的に勉強しました。アーキテクチャを設計する時は、まず RTO/RPO、アクセス量、データの機密性、予算などの要件を確認し、運用負荷を下げるためにマネージドサービスを優先して選択します。

### 关键词

- 高可用性（こうかようせい）：高可用
- 障害対応（しょうがいたいおう）：故障应对
- 運用負荷（うんようふか）：运维负担
- 要件（ようけん）：需求
- 費用対効果（ひようたいこうか）：性价比

---

## Web 三层架构

### 日本語回答

一般的な Web アプリケーションであれば、Route 53 で DNS を管理し、ALB をパブリックサブネットに配置します。アプリケーションサーバーはプライベートサブネットの Auto Scaling Group で複数 AZ に分散します。データベースは RDS Multi-AZ を利用して、AZ 障害時にも自動フェイルオーバーできるようにします。

### 中文解释

典型 Web：Route 53 → ALB → 私有子网 ASG → RDS Multi-AZ。强调多 AZ、私有子网、自动故障切换。

### 可追加一句

セキュリティ面では、EC2 に直接パブリック IP を付けず、Security Group で必要な通信だけを許可します。

安全上，不给 EC2 公网 IP，只用 SG 放行必要通信。

---

## 安全与 IAM

### 日本語回答

AWS リソースへアクセスするアプリケーションには、長期的なアクセスキーを使わず、IAM Role を付与します。また、最小権限の原則に基づいてポリシーを設計します。機密情報は Secrets Manager に保存し、必要に応じて KMS で暗号化します。

### 中文解释

应用访问 AWS 用 IAM Role，不用长期 AK/SK；最小权限；机密信息 Secrets Manager；加密 KMS。

### 面试常用句

- 最小権限の原則を意識しています。  
  我重视最小权限原则。
- アクセスキーのハードコーディングは避けるべきだと考えています。  
  我认为应该避免硬编码访问密钥。

---

## 私有网络访问 AWS 服务

### 日本語回答

プライベートサブネットの EC2 から S3 や DynamoDB にアクセスする場合、インターネットを経由させたくない要件であれば、VPC Endpoint を利用します。S3 と DynamoDB は Gateway Endpoint、それ以外の多くの AWS サービスは Interface Endpoint を利用します。

### 中文解释

私有子网访问 S3/DynamoDB 用 Gateway Endpoint；其他 AWS API 用 Interface Endpoint。强调不走公网和安全。

---

## 数据库高可用与读扩展

### 日本語回答

RDS の Multi-AZ は主に高可用性のために利用します。従来型の Multi-AZ DB インスタンスでは、単一のスタンバイは読み取り処理には利用しません。一方、Multi-AZ DB クラスターでは、2 台の読み取り可能なスタンバイを利用できます。Read Replica は主に読み取り負荷を分散するための仕組みです。要件とデプロイ方式を確認して選択します。

### 中文解释

Multi-AZ 主要解决高可用，但要区分传统单 standby 与具有两个可读 standby 的 Multi-AZ DB cluster；Read Replica 主要用于读扩展。

---

## 解耦设计

### 日本語回答

アクセスが急増する可能性がある処理では、SQS を使ってコンポーネント間を疎結合にします。プロデューサーはメッセージをキューに送信し、ワーカーは自分の処理能力に合わせてメッセージを取得します。失敗したメッセージは DLQ に送ることで、後から原因分析できます。

### 中文解释

流量突增/削峰：SQS 解耦，worker 按能力处理，失败进 DLQ。

### SNS/EventBridge 表达

複数のシステムへ同じ通知を配信したい場合は SNS を利用します。イベントの内容に応じてターゲットを振り分けたい場合は EventBridge を利用します。

一发多收 SNS；按事件内容路由 EventBridge。

---

## 静态内容全球加速

### 日本語回答

画像や静的ファイルをグローバルユーザーに低レイテンシーで配信する場合、S3 をオリジンとして CloudFront を利用します。S3 バケットを直接公開せず、Origin Access Control を使って CloudFront 経由のアクセスだけを許可する構成が望ましいです。

### 中文解释

S3 + CloudFront + OAC，低延迟且源站私有。

---

## 成本优化

### 日本語回答

コスト最適化では、ワークロードの特性に応じて購入オプションやストレージクラスを選びます。安定して長期間稼働する EC2 には Savings Plans や Reserved Instances を検討します。中断可能なバッチ処理には Spot Instances が有効です。S3 ではライフサイクルポリシーを使って、アクセス頻度の低いデータを Glacier 系のストレージクラスへ移行します。

### 中文解释

稳定 EC2：Savings Plans/RI；可中断：Spot；S3 冷数据：Lifecycle 到 Glacier。

---

## 灾备与 RTO/RPO

### 日本語回答

災害対策では、まず RTO と RPO を確認します。コストを最小化したい場合は Backup and Restore を選びます。より短い復旧時間が必要な場合は Pilot Light や Warm Standby を検討します。ほぼ停止を許容できない重要システムでは、Multi-site Active/Active 構成が候補になりますが、コストと運用の複雑さが高くなります。

### 中文解释

先问 RTO/RPO；成本最低 Backup；更快 Pilot Light/Warm Standby；几乎不停 Active/Active。

---

## 监控与审计

### 日本語回答

運用監視では CloudWatch を使ってメトリクス、ログ、アラームを管理します。API 操作の監査には CloudTrail を利用します。リソース設定の変更履歴やコンプライアンスチェックには AWS Config を利用します。

### 中文解释

CloudWatch 指标日志告警；CloudTrail API 审计；Config 配置合规。

---

## 面试中好用的连接句

- 要件によりますが、まずは〇〇を検討します。  
  取决于需求，但我会先考虑某服务。
- 運用負荷を下げるために、マネージドサービスを優先します。  
  为降低运维负担，我优先托管服务。
- セキュリティの観点では、〇〇が重要だと考えています。  
  从安全角度，我认为某点重要。
- コストと可用性のバランスを考える必要があります。  
  需要平衡成本和可用性。
- 単一障害点を避けるために、複数 AZ に分散します。  
  为避免单点故障，分散到多个 AZ。

## 简短项目说明模板

私は AWS 上で小規模な Web アプリケーションを想定し、可用性とセキュリティを意識した構成を設計しました。ALB と Auto Scaling Group を複数 AZ に配置し、データベースには RDS Multi-AZ を利用します。静的コンテンツは S3 と CloudFront で配信し、アプリケーションから AWS サービスへアクセスする場合は IAM Role を利用します。また、CloudWatch と CloudTrail を使って監視と監査を行います。

中文：我设计过一个小型 AWS Web 架构，考虑可用性和安全。ALB+ASG 多 AZ，RDS Multi-AZ，静态内容 S3+CloudFront，应用访问 AWS 用 IAM Role，CloudWatch/CloudTrail 做监控审计。
