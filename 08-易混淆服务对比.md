# 08 - SAA 易混淆服务对比

这份专门解决 SAA 选项里最容易二选一错的服务。记住：同样“能做”，考试要选最符合题干约束的。

## RDS Multi-AZ DB instance / DB cluster vs Read Replica

| 方案 | 高可用 | 可读副本 | 复制/切换重点 |
|---|---|---|---|
| Multi-AZ DB instance | 是 | 单 standby 不可读 | 同步 standby，自动故障切换 |
| Multi-AZ DB cluster | 是 | 2 个 readable standby | 3 AZ，自动故障切换并可扩展读取 |
| Read Replica | 不是自动 HA 首选 | 是 | 异步读扩展，可手动提升 |

考试老口诀“Multi-AZ 保命、Read Replica 分担读”通常针对传统 Multi-AZ DB instance。若题干明确写 **Multi-AZ DB cluster**，不能再用“standby 一定不可读”排除。

| 对比 | Multi-AZ | Read Replica |
|---|---|---|
| 主要目的 | 高可用/故障切换 | 读扩展/报表 |
| 复制 | 同步（通常） | 异步 |
| 是否承担读流量 | 通常不 | 是 |
| 故障切换 | 自动 | 通常需手动提升 |
| 高频关键词 | AZ failure, failover, minimize downtime | read-heavy, offload reads, reporting |

口诀：**Multi-AZ 保命，Read Replica 分担读。**

## Aurora Replica vs Aurora Global Database

- 同区域读扩展/高可用 → Aurora Replica。
- 跨区域低 RPO 灾备、全球低延迟读 → Aurora Global Database。
- 多区域都要写且 NoSQL → DynamoDB Global Tables，不是 Aurora。

## SQS vs SNS vs EventBridge

| 服务 | 交互模型 | 高频场景 |
|---|---|---|
| SQS | 队列，消费者拉取 | 削峰、异步 worker、失败重试 |
| SNS | Pub/Sub，主动推送 | 一发多收、通知、fanout |
| EventBridge | 事件总线+规则 | SaaS/AWS 事件、按内容路由、定时 |

- “处理不过来，排队慢慢处理” → SQS。
- “同一事件通知多个系统” → SNS。
- “根据事件字段匹配不同目标” → EventBridge。
- “一发多收且各自可靠消费” → SNS + 多个 SQS。

## SQS Standard vs FIFO

| 对比 | Standard | FIFO |
|---|---|---|
| 吞吐 | 最高 | 较低/可批量提升 |
| 顺序 | 尽力而为 | MessageGroupId 内严格有序 |
| 投递 | 至少一次，可能重复 | 去重能力，exactly-once processing 语义 |
| 场景 | 大多数削峰 | 金融订单、必须顺序处理 |

题目没强调顺序/去重，通常选 Standard。

## CloudWatch vs CloudTrail vs Config

| 需求 | 服务 |
|---|---|
| CPU、内存自定义指标、日志、告警 | CloudWatch |
| 谁调用了 DeleteBucket/StopInstances | CloudTrail |
| S3 是否公开、SG 是否开放 0.0.0.0/0、配置历史 | AWS Config |

口诀：**Watch 看运行状态，Trail 看 API 足迹，Config 看配置合规。**

## GuardDuty vs Inspector vs Macie vs Security Hub

- GuardDuty：威胁检测，异常 API、恶意 IP、可疑 DNS、账号被盗迹象。
- Inspector：漏洞和暴露扫描，EC2/ECR/Lambda。
- Macie：S3 里找敏感数据。
- Security Hub：聚合各安全服务发现，不是单独检测引擎。

## WAF vs Shield vs Security Group/NACL

| 服务 | 层级 | 场景 |
|---|---|---|
| WAF | L7 Web | SQLi/XSS/Bot/IP 规则，挂 CloudFront/ALB/API Gateway |
| Shield | DDoS | 标准/高级 DDoS 防护 |
| Security Group | 实例/ENI | 允许入出站端口，stateful |
| NACL | 子网 | allow/deny，stateless，显式拒绝 IP |

Web 攻击选 WAF；DDoS 选 Shield；网络端口控制选 SG/NACL。

## NAT Gateway vs VPC Endpoint

- 私有子网访问互联网任意地址 → NAT Gateway。
- 私有访问 S3/DynamoDB → Gateway Endpoint，更安全更便宜。
- 私有访问 AWS API/第三方 PrivateLink 服务 → Interface Endpoint。
- 题目强调“不经过公网/private connectivity”时，Endpoint 优先于 NAT。

## Internet Gateway vs NAT Gateway

- IGW：让公有子网资源入/出互联网。
- NAT Gateway：让私有子网资源出互联网，不允许互联网主动进来。
- 私有实例通过 NAT 出网时，NAT 放公有子网，私有路由表指向 NAT。

## VPC Peering vs Transit Gateway vs PrivateLink

| 服务 | 场景 | 限制/特点 |
|---|---|---|
| VPC Peering | 少量 VPC 互通 | 不支持传递路由，不可重叠 CIDR |
| Transit Gateway | 多 VPC/本地中心互联 | Hub-and-spoke，可路由集中管理 |
| PrivateLink | 只暴露某个服务给消费者 | 消费者不需要访问整个 VPC |

## ALB vs NLB vs GWLB

- ALB：HTTP/HTTPS、路径/主机路由、OIDC/Cognito、容器服务。
- NLB：TCP/UDP/TLS、极高性能、静态 IP、源 IP 保留。
- GWLB：第三方虚拟防火墙/安全设备透明插入。

题目说 URL path `/api` → ALB；说 UDP/固定 IP/百万连接 → NLB；说 appliance/firewall inspection → GWLB。

## CloudFront vs Global Accelerator vs Route 53

| 服务 | 作用 |
|---|---|
| CloudFront | 边缘缓存 HTTP(S) 内容，降低源站压力 |
| Global Accelerator | Anycast 静态 IP，优化 TCP/UDP 到区域端点，不缓存 |
| Route 53 | DNS 解析和路由策略，DNS TTL 影响切换速度 |

静态图片视频 → CloudFront；游戏 TCP/UDP 全球入口 → Global Accelerator；DNS 权重/延迟/主备 → Route 53。

## S3 Replication vs Backup vs Lifecycle

- Replication：复制对象到同/跨区域，灾备/合规/近用户；需要 Versioning。
- Backup：按备份策略保留恢复点，防误删/勒索更像备份。
- Lifecycle：自动转低成本存储类或删除，成本优化。

复制不是完整备份策略：删除标记、历史版本、恶意覆盖要结合 Versioning/Object Lock/Backup。

## S3 Standard-IA vs One Zone-IA vs Glacier

- Standard-IA：低频，仍要多 AZ 可用和毫秒访问。
- One Zone-IA：可重建数据，单 AZ，成本更低。
- Glacier Instant：归档但要毫秒访问。
- Glacier Flexible/Deep Archive：恢复慢，成本低。

## EBS vs EFS vs FSx vs S3

| 服务 | 类型 | 关键词 |
|---|---|---|
| EBS | 块 | 单 EC2 磁盘、数据库卷、低延迟 IOPS |
| EFS | 文件 NFS | 多 Linux EC2/容器共享，多 AZ |
| FSx Windows | 文件 SMB | Windows 文件服务器、AD、SMB |
| FSx Lustre | 文件 HPC | 高吞吐、机器学习、HPC、S3 联动 |
| S3 | 对象 | 静态资源、备份、数据湖、无限扩展 |

## EBS gp3 vs io2

- gp3：大多数通用工作负载，IOPS/吞吐可独立配置，成本优。
- io2/io2 Block Express：关键数据库、高 IOPS、低延迟、高耐久。
- st1/sc1：吞吐型 HDD/冷 HDD，不能做启动盘。

## EFS Standard vs EFS One Zone / IA

- Standard：多 AZ，高可用。
- One Zone：单 AZ，成本低，适合可重建或非生产。
- IA：低频文件自动降成本，配合生命周期。

## RDS/Aurora vs DynamoDB

- 需要 SQL、JOIN、事务、关系模型 → RDS/Aurora。
- Key-value 高并发、简单查询、Serverless、毫秒级 → DynamoDB。
- 全球多区域多主写 → DynamoDB Global Tables。
- 关系型全球灾备低 RPO → Aurora Global Database。

## DynamoDB DAX vs ElastiCache

- DAX：只给 DynamoDB 用的读缓存，微秒级，API 兼容。
- ElastiCache：通用缓存/会话/排行榜/发布订阅，可用于 RDS 或应用层。

## Athena vs Redshift vs OpenSearch

- Athena：S3 上直接 SQL，Serverless，适合偶发/低运维查询。
- Redshift：数据仓库，复杂 OLAP、高并发 BI。
- OpenSearch：全文搜索、日志检索、近实时分析。

## Kinesis Data Streams vs Firehose vs SQS

- Kinesis Data Streams：实时流，多消费者，自管 shard，毫秒级处理。
- Firehose：托管投递到 S3/Redshift/OpenSearch，近实时，运维少。
- SQS：消息队列，不是流分析平台。

## DMS vs DataSync vs Storage Gateway vs Snowball

| 服务 | 场景 |
|---|---|
| DMS | 数据库迁移，CDC，低停机 |
| DataSync | 在线文件同步 NFS/SMB/HDFS/S3/EFS/FSx |
| Storage Gateway | 本地应用继续用文件/块/磁带接口访问云 |
| Snowball | 大数据量离线迁移，带宽不足 |

## Backup and Restore vs Pilot Light vs Warm Standby vs Active/Active

- Backup and Restore：成本最低，RTO/RPO 最长。
- Pilot Light：核心数据/最小关键服务常亮，灾难时补齐。
- Warm Standby：缩小版完整系统常亮，灾难时扩容。
- Active/Active：两地同时服务，成本最高，恢复最快。

## Savings Plans vs Reserved Instances vs Spot

- Savings Plans：承诺每小时花费，覆盖 EC2/Fargate/Lambda（Compute SP），灵活。
- RI：承诺特定实例属性，适合稳定 RDS/EC2。
- Spot：可中断最低价，不适合必须持续运行的数据库主节点。

## CloudFormation vs Elastic Beanstalk vs OpsWorks

- CloudFormation/CDK：基础设施即代码，精细控制。
- Elastic Beanstalk：开发者上传应用，平台帮管环境，控制较少。
- OpsWorks：Chef/Puppet 配置管理，SAA 低频。

## Cognito vs IAM Identity Center

- Cognito：应用用户注册/登录，移动/Web App 用户池。
- IAM Identity Center：员工/组织成员登录 AWS 多账号和业务应用。

## Transfer Acceleration vs DataSync vs Transfer Family

- S3 Transfer Acceleration：全球用户上传/下载 S3 加速。
- DataSync：批量/持续迁移文件数据。
- Transfer Family：给外部伙伴提供 SFTP/FTPS/FTP 接入 S3/EFS。
