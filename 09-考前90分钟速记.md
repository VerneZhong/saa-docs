# 09 - SAA 考前 90 分钟速记

考前只看判断规则，不再钻细节。目标是把常见关键词和服务选择固定住。

## 0-10 分钟：考试策略

1. 先看最后一句：问成本、可靠、安全、性能、运维最少、迁移？
2. 看到绝对词要小心：always、only、must manually、自建脚本通常不是最佳实践。
3. 两个都能做时，优先：托管服务 > 自建；多 AZ > 单 AZ；Role > Access Key；Endpoint > 公网。
4. “least operational overhead” 强烈偏 Serverless/托管。
5. “cost-effective” 是满足约束后最省，不是牺牲高可用/安全。

## 10-25 分钟：安全

- 应用访问 AWS：**IAM Role**，不要硬编码 AK/SK。
- 跨账号：**STS AssumeRole**；第三方加 **External ID**。
- 多账号统一限制：**Organizations SCP**。
- 员工统一登录 AWS 多账号：**IAM Identity Center**。
- 密钥管理：**KMS**；密码自动轮换：**Secrets Manager**；普通配置：**SSM Parameter Store**。
- API 审计谁做了什么：**CloudTrail**。
- 配置合规：**AWS Config**。
- 指标/日志/告警：**CloudWatch**。
- Web 攻击 SQLi/XSS：**WAF**。
- DDoS：**Shield**。
- 威胁检测：**GuardDuty**。
- S3 敏感数据：**Macie**。
- 漏洞扫描：**Inspector**。

## 25-40 分钟：网络和加速

- 公有子网出入互联网：**Internet Gateway**。
- 私有子网主动出互联网：**NAT Gateway**。
- 私有访问 S3/DynamoDB：**Gateway VPC Endpoint**。
- 私有访问其他 AWS 服务：**Interface Endpoint/PrivateLink**。
- 两 VPC 简单互联：**VPC Peering**，无传递路由。
- 多 VPC/本地中心互联：**Transit Gateway**。
- 快速加密混合连接：**Site-to-Site VPN**。
- 稳定低延迟专线：**Direct Connect**；要加密就 DX + VPN。
- HTTP/HTTPS 路由：**ALB**。
- TCP/UDP/固定 IP/超高性能：**NLB**。
- 安全设备流量检查：**GWLB**。
- 静态/HTTP 内容全球缓存：**CloudFront**。
- TCP/UDP 全球入口 Anycast IP：**Global Accelerator**。
- DNS 故障切换/延迟/权重：**Route 53 routing policies**。

## 40-55 分钟：计算和存储

### 计算

- 传统服务器、控制 OS：**EC2**。
- EC2 自动扩缩：**ASG**。
- Docker 少运维：**ECS Fargate**。
- 必须 Kubernetes：**EKS**。
- 事件驱动短任务：**Lambda**。
- 无服务器 API：**API Gateway + Lambda**。
- 长期稳定计算：**Savings Plans/RI**。
- 可中断批处理：**Spot**。
- BYOL/物理主机：**Dedicated Host**。

### 存储

- 对象/静态网站/数据湖：**S3**。
- 单 EC2 块存储：**EBS**。
- 临时本地高速：**Instance Store**。
- 多 Linux EC2 共享：**EFS**。
- Windows SMB：**FSx for Windows**。
- HPC/ML 高吞吐：**FSx for Lustre**。
- 本地继续用 NFS/SMB/iSCSI/磁带接口：**Storage Gateway**。

### S3 口诀

- 访问未知：**Intelligent-Tiering**。
- 低频但快速：**Standard-IA**。
- 可重建低频：**One Zone-IA**。
- 归档但毫秒：**Glacier Instant**。
- 归档分钟/小时：**Glacier Flexible**。
- 最低成本长期归档：**Deep Archive**。
- 防误删：Versioning / MFA Delete / Object Lock。
- 跨区域：CRR。
- 自动降成本：Lifecycle。
- 临时访问：Presigned URL。

## 55-70 分钟：数据库、缓存、分析

- 关系型托管：**RDS**。
- RDS 高可用故障切换：**Multi-AZ**。
- RDS 读扩展：**Read Replica**。
- AWS 云原生关系库：**Aurora**。
- 跨区域关系型低 RPO：**Aurora Global Database**。
- Lambda 连接 RDS 爆炸：**RDS Proxy**。
- NoSQL 高并发键值：**DynamoDB**。
- DynamoDB 不可预测流量：**On-demand**。
- DynamoDB 全球多主：**Global Tables**。
- DynamoDB 缓存：**DAX**。
- 会话/排行榜/通用缓存：**ElastiCache Redis/Valkey**。
- S3 上 SQL：**Athena**。
- ETL/Data Catalog：**Glue**。
- 数据仓库 OLAP：**Redshift**。
- 日志全文搜索：**OpenSearch**。
- 实时流：**Kinesis Data Streams**；托管投递：**Firehose**。

## 70-80 分钟：解耦、迁移、灾备

### 解耦

- 削峰队列：**SQS**。
- 严格顺序：**SQS FIFO**。
- 一发多收：**SNS**。
- 一发多收且各自缓冲：**SNS + SQS**。
- 事件规则/SaaS/定时：**EventBridge**。
- 多步骤流程/重试/状态：**Step Functions**。
- 失败消息隔离：**DLQ**。

### 迁移

- 数据库迁移低停机：**DMS**。
- 在线文件同步：**DataSync**。
- TB/PB 带宽不足：**Snowball**。
- 托管 SFTP/FTPS/FTP：**Transfer Family**。
- 服务器迁移：**Application Migration Service**。

### 灾备

- 成本最低，慢恢复：**Backup and Restore**。
- 核心组件常亮，灾难时补齐：**Pilot Light**。
- 缩小版全栈常亮：**Warm Standby**。
- 两地同时服务：**Active/Active**。

## 80-90 分钟：最后易错清单

- Multi-AZ 不是读扩展；Read Replica 不是 HA 首选。
- NAT Gateway 不是私网访问 AWS 服务的最安全/最低成本答案；Endpoint 才是。
- CloudTrail 不是指标告警；CloudWatch 不是 API 审计；Config 不是日志。
- SQS 是队列，SNS 是广播，EventBridge 是规则路由。
- CloudFront 缓存 HTTP 内容；Global Accelerator 不缓存。
- VPC Peering 不支持传递路由；多 VPC 用 Transit Gateway。
- S3 Lifecycle 降成本；Replication 做复制；Object Lock 做 WORM。
- DynamoDB Global Tables 多区域多活；Aurora Global Database 是关系型跨区域灾备/读。
- WAF 防 Web L7；Shield 防 DDoS；GuardDuty 发现威胁。
- 题目说“least operational overhead”不要选自建 EC2 集群。

## 考场心法

不会的题先标记。很多 SAA 题不是靠记忆，而是靠排除反模式：单点、明文、长期密钥、公网暴露、手工脚本、自建复杂系统。先排掉它们，正确答案通常就很清楚。
