# 11 - 查漏补缺与边界条件

这份清单专门补充“口诀容易说过头”的边界条件，并按 SAA-C03 四大领域提示最后查漏。先掌握主规则，再记例外；不要因为例外而失去做题速度。

## 本轮审校结论

- 四大领域权重与官方 SAA-C03 Exam Guide 一致：安全 30%、弹性 26%、高性能 24%、成本 20%。
- 原笔记的主体结构正确，IAM、VPC、存储、数据库、解耦、监控、迁移和灾备均有覆盖。
- 最大风险不是“漏掉整个领域”，而是把高频口诀写成绝对规则，尤其是 RDS Multi-AZ、SQS FIFO、跨区域灾备和成本优化。
- 模拟题适合入门巩固，但难度偏基础；复习后期应补充多约束题、组合题和“两个答案都能做但一个更少运维”的题型。

## 必须记住的边界条件

### 1. RDS Multi-AZ 有两种形态

- **Multi-AZ DB instance**：一个 primary + 一个同步 standby；standby 不提供读流量。
- **Multi-AZ DB cluster**：一个 writer + 两个 readable standby，分布在三个 AZ；既提供自动故障切换，也能增加读容量。
- **Read Replica**：典型读扩展方案，通常异步复制；不是自动高可用的首选答案。

官方依据：[RDS Multi-AZ deployments](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.MultiAZ.html)。

### 2. SQS FIFO 的“Exactly-once”有条件

- FIFO 通过 deduplication ID 或内容去重，避免生产者重试在去重窗口内引入重复消息。
- 顺序保证发生在同一个 `MessageGroupId` 内；多个 group 可以并行处理。
- 消费端仍应设计幂等，因为应用可能在处理完成、删除消息之前失败。
- FIFO 不是简单等于“很慢”；high-throughput mode 可以提升吞吐。

官方依据：[SQS FIFO exactly-once processing](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-exactly-once-processing.html)。

### 3. 高可用、备份与灾备不是一回事

- Multi-AZ 主要处理同区域 AZ/实例故障，不等于跨区域灾备。
- Read Replica/Global Database 可以降低跨区域 RPO，但仍要设计切换、写入端点和数据一致性。
- Replication 会复制变更，不能自动替代独立恢复点；防误删或恶意覆盖还要考虑 Versioning、Object Lock、AWS Backup/Vault Lock。
- 先从题目读出 RTO、RPO、可接受成本，再选 Backup and Restore、Pilot Light、Warm Standby 或 Active/Active。

### 4. “私网”与“加密”要分开判断

- Direct Connect 是专用连接，但默认不等于端到端加密；题目要求加密时考虑 VPN over DX 或支持的 MACsec 场景。
- VPC Endpoint 让流量不经公共互联网，但权限仍需要 IAM policy、endpoint policy 和资源策略共同约束。
- Security Group 是 stateful allow-only；NACL 是 stateless allow/deny。WAF 解决 L7 Web 攻击，不能代替网络访问控制。

### 5. 成本题要包含数据传输

- NAT Gateway 除小时费外还有数据处理费；大量访问 S3/DynamoDB 时 Gateway Endpoint 往往更合适。
- 跨 AZ、跨区域和公网出口可能产生数据传输费用；“多 AZ 更可靠”不代表可以忽略无必要的跨 AZ 流量。
- CloudFront 不只降低延迟，也可能通过缓存和 AWS 骨干网减少源站及公网出口压力。
- 稳定用量优先比较 Savings Plans/RI；可中断任务用 Spot；容量保证与价格折扣是两个不同问题。

## 四大领域最后检查

### 安全架构（30%）

- [ ] 能区分 identity policy、resource policy、permissions boundary、SCP。
- [ ] 会用 IAM Identity Center、AssumeRole、External ID 处理员工与跨账号访问。
- [ ] 能区分 KMS、Secrets Manager、Parameter Store、CloudHSM。
- [ ] 能区分 WAF、Shield、GuardDuty、Inspector、Macie、Security Hub。
- [ ] 知道 CloudTrail、Config、CloudWatch 分别回答审计、配置、可观测性问题。

### 弹性架构（26%）

- [ ] 会设计跨 AZ 的 ALB + ASG + 数据层，并识别单点。
- [ ] 能区分 RDS 两种 Multi-AZ、Read Replica 与 Aurora Global Database。
- [ ] 会用 SQS、SNS、EventBridge、Step Functions 做解耦和故障隔离。
- [ ] 能根据 RTO/RPO 选择四种灾备策略。
- [ ] 知道备份、复制、Versioning、Object Lock 各解决什么问题。

### 高性能架构（24%）

- [ ] 能按协议和缓存需求区分 ALB、NLB、CloudFront、Global Accelerator。
- [ ] 能按访问模式选择 EBS、EFS、FSx、S3 及存储层。
- [ ] 会选 Read Replica、ElastiCache、DAX、RDS Proxy。
- [ ] 能区分 Athena、Redshift、OpenSearch、Kinesis Data Streams、Firehose。
- [ ] 知道分区键、缓存命中和连接池对性能的影响。

### 成本优化架构（20%）

- [ ] 会比较 On-Demand、Savings Plans、RI、Spot、Capacity Reservation。
- [ ] 会用 S3 Lifecycle、Intelligent-Tiering 和 EBS gp3 优化成本。
- [ ] 能把 NAT、跨 AZ/区域、CloudFront 等数据传输费用纳入判断。
- [ ] 能在满足安全、RTO/RPO 和性能后再选最低成本，而非只挑单价最低。

## 官方复习入口

- [SAA-C03 Exam Guide](https://docs.aws.amazon.com/aws-certification/latest/solutions-architect-associate-03/solutions-architect-associate-03.html)
- [SAA-C03 in-scope services](https://docs.aws.amazon.com/aws-certification/latest/solutions-architect-associate-03/saa-03-in-scope-services.html)
- [AWS Well-Architected Framework](https://docs.aws.amazon.com/wellarchitected/latest/framework/welcome.html)

> 本资料用于帮助理解和复习，不是 AWS 官方题库。服务能力和考试范围可能更新，临考前应再核对官方 Exam Guide。
