# 06 - SAA-C03 完整考点笔记

目标：按 SAA-C03 四个考试领域整理一套“做题能用”的完整笔记。不要把它当官方文档背，而要练成：看到题干约束 → 识别目标 → 选最托管、最安全、最便宜或最可靠的方案。

> 审校基准：以 AWS 官方 SAA-C03 Exam Guide 与服务文档为准。服务能力会变化，口诀必须结合题目明确的部署类型和约束使用。参见 [SAA-C03 Exam Guide](https://docs.aws.amazon.com/aws-certification/latest/solutions-architect-associate-03/solutions-architect-associate-03.html)。

## 考试领域与做题优先级

| 领域 | 大致权重 | 做题时要问自己 |
|---|---:|---|
| Design Secure Architectures | 30% | 身份、权限、加密、网络边界、审计是否安全？ |
| Design Resilient Architectures | 26% | 单点故障、AZ/Region 故障、备份恢复、解耦是否处理？ |
| Design High-Performing Architectures | 24% | 延迟、吞吐、扩展、缓存、读写分离是否合适？ |
| Design Cost-Optimized Architectures | 20% | 生命周期、计费模式、托管服务、按需/预留/Spot 是否最省？ |

通用排除法：硬编码密钥、单实例生产、公开数据库、自建替代托管服务、跨 AZ 流量无必要、长期使用 On-Demand、把审计/监控服务混用，通常都是错项。

---

## 1. 身份、安全与治理

### IAM：SAA 最高频安全基础

核心对象：User、Group、Role、Policy。考试重点不是语法，而是“谁临时扮演什么角色访问什么资源”。

- **IAM Role**：EC2、Lambda、ECS Task、跨账号访问的首选。题目出现“avoid long-term credentials / no hardcoded keys”几乎选 Role。
- **IAM Policy**：Identity-based policy 绑在人/角色上；Resource-based policy 绑在资源上，例如 S3 bucket policy、KMS key policy、SQS queue policy。
- **STS AssumeRole**：跨账号、临时凭证、联合身份常见答案。
- **IAM Identity Center**：企业人员统一登录多账号，替代给每个人创建 IAM User。
- **Permission Boundary**：限制开发者能创建的最大权限，常用于委派管理。
- **SCP（Organizations）**：组织/OU/账号级权限上限；不能授予权限，只能限制。
- **Access Analyzer**：发现外部可访问资源、验证策略是否过宽。

做题判断：

- 应用访问 AWS 服务 → IAM Role，不要 AK/SK。
- 多账号统一限制“任何账号不得关闭 CloudTrail” → Organizations SCP。
- 第三方/另一个账号访问本账号资源 → AssumeRole + External ID（防 confused deputy）。
- 临时给员工 AWS 控制台访问 → IAM Identity Center + Permission Set。

### KMS 与加密

- **KMS key**：管理加密密钥，支持自动轮换、审计、Key policy。
- **AWS managed key**：省事，控制较少；**Customer managed key**：需要精细权限、跨账号、审计时选。
- **Envelope encryption**：数据密钥加密数据，KMS 加密数据密钥。
- **CloudHSM**：必须自己控制 HSM、FIPS/合规极严才选；SAA 少见。

常见组合：

- S3 SSE-S3：最少运维的服务端加密。
- S3 SSE-KMS：需要 KMS 权限控制和 CloudTrail 审计。
- EBS/RDS/EFS/CloudWatch Logs 都可用 KMS 加密。
- 跨账号访问 KMS 加密对象：S3 policy + KMS key policy/IAM 都要允许。

### Secrets Manager / Parameter Store

| 场景 | 优先服务 |
|---|---|
| RDS 密码、API 密钥、自动轮换 | Secrets Manager |
| 普通配置、层级参数、低成本 | SSM Parameter Store |
| 加密参数 | Parameter Store SecureString + KMS |

题目强调“automatic rotation”选 Secrets Manager；只说集中存配置且便宜，可选 Parameter Store。

### Organizations / Control Tower / 多账号

- **AWS Organizations**：统一账单、账号治理、SCP。
- **Consolidated Billing**：合并账单，聚合用量折扣。
- **Control Tower**：快速搭 landing zone，多账号合规基线。
- **RAM**：跨账号共享资源，例如 Transit Gateway、子网、Route 53 Resolver 规则。

### 安全检测与边界

- **WAF**：Web L7 防护，SQL injection、XSS、IP/地理匹配，常挂 CloudFront/ALB/API Gateway。
- **Shield Standard**：默认 DDoS 基础防护；**Shield Advanced**：高级 DDoS、成本保护、DRT。
- **GuardDuty**：威胁检测，分析 CloudTrail/VPC DNS/Flow Logs/EKS 等，发现可疑行为。
- **Inspector**：漏洞扫描，EC2/ECR/Lambda 包漏洞。
- **Macie**：S3 敏感数据发现（PII）。
- **Security Hub**：聚合安全发现和合规状态。

做题判断：

- 防 SQL 注入/XSS → WAF。
- 发现账号异常 API、挖矿、恶意 IP 通信 → GuardDuty。
- 扫 EC2/ECR 漏洞 → Inspector。
- 找 S3 中身份证/信用卡等敏感数据 → Macie。

---

## 2. 网络与内容分发

### VPC 基础

- **VPC**：区域级逻辑网络。
- **Subnet**：AZ 级；高可用通常至少 2 个 AZ。
- **Route Table**：决定下一跳。
- **Internet Gateway**：公有子网入/出互联网。
- **NAT Gateway**：私有子网主动出互联网；部署在公有子网；生产高可用要每 AZ 一个 NAT。
- **Egress-only Internet Gateway**：IPv6 出站。

公有子网 = 有到 IGW 路由 + 实例有公网 IP。只有 IGW 不够。

### Security Group vs NACL

| 对比 | Security Group | NACL |
|---|---|---|
| 位置 | ENI/实例级 | 子网级 |
| 状态 | Stateful | Stateless |
| 规则 | 只允许 Allow | Allow + Deny |
| 高频用途 | 应用端口控制 | 子网边界、显式拒绝 IP |

如果题目说“deny a specific IP at subnet boundary”，NACL 更合适；一般实例访问控制选 SG。

### VPC 连接

- **VPC Peering**：两个 VPC 点对点，不支持传递路由，不重叠 CIDR。
- **Transit Gateway**：多 VPC/本地网络 hub-and-spoke，大规模首选。
- **PrivateLink / Interface Endpoint**：私网访问服务，不暴露公网；消费者通过 ENI 访问提供方服务。
- **Gateway Endpoint**：S3/DynamoDB 私网访问，通常比 NAT 便宜。
- **Route 53 Resolver**：混合 DNS 查询转发。

### 混合网络

- **Site-to-Site VPN**：公网 IPSec，快、便宜、加密，但延迟/带宽受互联网影响。
- **Direct Connect**：专线、稳定低延迟，不默认加密；需要加密可 DX + VPN。
- **Client VPN**：远程用户接入 VPC。

题目关键词：快速上线/低成本加密 → VPN；稳定私有低延迟大带宽 → Direct Connect；多 VPC + 本地中心化 → Transit Gateway。

### Route 53

- **A/AAAA/CNAME/Alias**：Alias 可指向 ALB、CloudFront、S3 website 等，根域可用 Alias。
- **Routing policy**：Simple、Weighted、Latency、Failover、Geolocation、Geoproximity、Multivalue answer。
- **Health Check**：DNS 级故障切换。
- **Private Hosted Zone**：VPC 内部 DNS。

做题判断：

- 蓝绿/金丝雀 DNS 分流 → Weighted routing。
- 用户访问最近低延迟区域 → Latency routing。
- 主备 DNS 故障切换 → Failover routing + health check。
- 根据国家/地区合规路由 → Geolocation。

### ELB / CloudFront / Global Accelerator

| 服务 | 适合 |
|---|---|
| ALB | HTTP/HTTPS，路径/主机路由，WebSocket，容器目标 |
| NLB | TCP/UDP/TLS，极低延迟，高吞吐，静态 IP |
| GWLB | 透明插入第三方防火墙/安全设备 |
| CloudFront | HTTP(S) 内容分发和缓存，边缘 TLS，OAC 保护 S3 |
| Global Accelerator | 非缓存 TCP/UDP 全球入口，Anycast 静态 IP，区域故障转移 |

CloudFront 可以缓存动态/静态 HTTP 内容；Global Accelerator 不缓存，优化网络路径，适合游戏、IoT、非 HTTP 或需要固定全球 IP 的应用。

---

## 3. 计算服务

### EC2 与购买选项

| 选项 | 题目关键词 |
|---|---|
| On-Demand | 短期、不确定、不能中断 |
| Reserved Instances | 实例族/区域稳定，1/3 年承诺 |
| Savings Plans | 稳定计算用量，灵活省钱，SAA 常优先于 RI |
| Spot | 可中断、批处理、队列 worker、最低成本 |
| Dedicated Host | 自带许可证、物理主机可见性、严格合规 |
| Capacity Reservation | 必须保证某 AZ 容量，不一定省钱 |

### Auto Scaling Group

- 多 AZ 部署，替换不健康实例。
- Target tracking：维持 CPU/ALB 请求数等目标。
- Step/Simple scaling：按阈值逐步扩。
- Scheduled scaling：可预测峰值。
- Lifecycle hook：扩缩容时执行初始化/清理。

常见架构：ALB + ASG across AZs + EC2 private subnets + RDS Multi-AZ。

### ECS / EKS / Fargate

- **ECS**：AWS 原生容器编排；运维少。
- **EKS**：托管 Kubernetes；题目已有 Kubernetes 生态/标准化才选。
- **Fargate**：无服务器运行容器，不管 EC2；最少运维。
- **ECR**：容器镜像仓库。

做题判断：

- 已有 Docker，想少管服务器 → ECS on Fargate。
- 必须 Kubernetes API/生态 → EKS。
- 需要控制宿主机/特殊 daemon → ECS/EKS on EC2。

### Lambda / API Gateway

- Lambda 适合事件驱动、短任务、自动扩展。
- 限制：最长执行时间、部署包/临时存储限制、冷启动、并发限制。
- API Gateway：REST/HTTP/WebSocket API，节流、鉴权、缓存。
- Lambda in VPC 访问私有资源；访问公网需 NAT。

题目出现“unpredictable traffic / no server management / event driven”优先 Lambda。长时间视频转码/HPC 不选 Lambda。

---

## 4. 存储服务

### S3

关键能力：高耐久对象存储、Versioning、Lifecycle、Replication、Event Notification、Presigned URL、Object Lock、Inventory、Access Points。

| 存储类 | 做题场景 |
|---|---|
| Standard | 高频访问、低延迟 |
| Standard-IA | 低频但需要毫秒访问，多 AZ |
| One Zone-IA | 可重建、低频、单 AZ 省钱 |
| Intelligent-Tiering | 访问模式未知，自动优化 |
| Glacier Instant Retrieval | 归档但偶尔要毫秒访问 |
| Glacier Flexible Retrieval | 分钟到小时恢复 |
| Glacier Deep Archive | 最低成本，小时级恢复，长期保留 |

高频判断：

- 防误删：Versioning + MFA Delete / Object Lock。
- 合规 WORM：Object Lock governance/compliance mode。
- 跨区域灾备：CRR，必须开启 Versioning。
- 临时授权下载/上传：Presigned URL。
- 全球上传加速：S3 Transfer Acceleration（边缘网络）。
- S3 私有源站给 CloudFront：OAC（新优先）/OAI。

### EBS / Instance Store / EFS / FSx

- **EBS**：块存储，AZ 级，给单 EC2 挂载；快照到 S3；gp3 通用，io2 高 IOPS。
- **Instance Store**：本地临时盘，实例停止/终止数据丢失；缓存/临时数据。
- **EFS**：Linux NFS，多 AZ 共享，弹性；Standard/IA，General Purpose/Max I/O。
- **FSx for Windows**：SMB、AD、Windows 文件共享。
- **FSx for Lustre**：HPC/机器学习，高吞吐，可联动 S3。
- **FSx for NetApp ONTAP/OpenZFS**：企业迁移特定文件系统能力。

### Storage Gateway

| 类型 | 场景 |
|---|---|
| File Gateway | 本地 NFS/SMB 访问 S3 对象 |
| Volume Gateway | 本地 iSCSI 块存储，快照到 AWS |
| Tape Gateway | 替代磁带备份 |

题目说“本地应用不能改造，继续用文件/块/磁带接口，同时把数据放 AWS”选 Storage Gateway。

---

## 5. 数据库与分析

### RDS / Aurora

- **RDS Multi-AZ DB instance**：同步单 standby，自动故障切换；standby 不承担读流量。
- **RDS Multi-AZ DB cluster**：1 个 writer + 2 个不同 AZ 的 readable standby，同时提供高可用与读容量。
- **Read Replica**：异步复制，读扩展/报表，可跨区域；手动提升。
- **Automated Backup + PITR**：按时间点恢复。
- **RDS Proxy**：连接池，保护数据库免受 Lambda 突发连接打爆。
- **Aurora**：云原生共享存储，最多 15 个副本，故障切换快。
- **Aurora Serverless v2**：容量自动缩放，适合波动负载。
- **Aurora Global Database**：跨区域低延迟读、低 RPO 灾备。

### DynamoDB

- Key-value/document，单位数毫秒，Serverless。
- **On-demand**：流量不可预测。
- **Provisioned + Auto Scaling**：可预测且想控成本。
- **DAX**：微秒级读缓存，只适合最终一致读。
- **Global Tables**：多区域多活。
- **Streams**：捕获变更，触发 Lambda。
- **TTL**：自动过期删除。

设计重点：分区键要高基数、均匀分布，避免 hot partition。

### 缓存

- **ElastiCache for Redis/Valkey**：复杂数据结构、排序、pub/sub、持久化、复制、会话存储、排行榜。
- **Memcached**：简单缓存、横向分片、无持久化。
- **CloudFront cache**：边缘缓存 HTTP 内容。
- **DAX**：DynamoDB 专用缓存。

### 分析与搜索

| 服务 | 场景 |
|---|---|
| Athena | S3 数据直接 SQL，Serverless，偶发查询 |
| Glue | ETL、Data Catalog、Crawler |
| Redshift | 数据仓库 OLAP，高并发分析；RA3/Serverless |
| OpenSearch | 日志全文搜索、近实时检索、可视化 |
| Kinesis Data Streams | 实时流摄取，自管 shard |
| Kinesis Firehose | 托管投递到 S3/Redshift/OpenSearch |
| EMR | Hadoop/Spark 大数据集群 |
| QuickSight | BI 可视化 |

做题判断：S3 上临时查日志 → Athena；企业数据仓库 → Redshift；应用日志搜索 → OpenSearch；实时点击流处理 → Kinesis。

---

## 6. 解耦、集成与工作流

### SQS

- Standard：最高吞吐、至少一次、尽力排序。
- FIFO：MessageGroupId 内严格顺序，并通过去重机制支持 exactly-once processing；high-throughput mode 可提高吞吐。
- Visibility Timeout：消费者处理期间隐藏消息；太短会重复处理。
- DLQ：隔离失败消息。
- Long polling：降低空轮询成本。

SQS 常和 ASG 组合：根据 queue depth 扩 worker。

### SNS

发布订阅，一发多收；支持 HTTP/S、Email、SMS、Lambda、SQS。扇出架构常用 SNS → 多个 SQS，让每个消费者独立处理和重试。

### EventBridge

事件总线、规则匹配、SaaS/AWS 事件、定时任务。比 SNS 更适合“按事件内容路由到不同目标”。

### Step Functions

多步骤工作流、状态管理、重试、补偿、人工审核。Standard 适合长流程/审计；Express 适合高吞吐短流程。

---

## 7. 监控、审计与运维

| 需求 | 服务 |
|---|---|
| 指标、日志、告警、Dashboard | CloudWatch |
| API 调用审计，谁做了什么 | CloudTrail |
| 资源配置历史与合规规则 | AWS Config |
| 自动修复/运维命令 | Systems Manager |
| 基础设施模板 | CloudFormation/CDK |
| 备份策略集中管理 | AWS Backup |
| 成本预算告警 | AWS Budgets/Cost Explorer |
| 架构建议 | Trusted Advisor / Well-Architected Tool |

注意：CloudWatch Logs 看应用日志；CloudTrail 看 AWS API；Config 看资源配置变化。这三个常被混淆。

---

## 8. 迁移与传输

- **DMS**：数据库迁移，同构/异构，支持 CDC 持续复制。
- **Schema Conversion Tool**：异构数据库 schema 转换。
- **Application Migration Service (MGN)**：服务器 lift-and-shift。
- **DataSync**：在线文件传输，本地 NFS/SMB/HDFS 到 S3/EFS/FSx。
- **Snowball Edge**：TB/PB 离线迁移，带宽不足时。
- **Transfer Family**：托管 SFTP/FTPS/FTP，后端 S3/EFS。
- **Migration Hub**：迁移进度跟踪。

---

## 9. 灾备策略

| 策略 | 成本 | RTO/RPO | 典型架构 |
|---|---|---|---|
| Backup and Restore | 最低 | 小时级或更久 | AWS Backup/S3/快照，灾难时恢复 |
| Pilot Light | 低 | 分钟到小时 | 只保留数据库/核心组件，计算灾难时扩容 |
| Warm Standby | 中 | 分钟级 | 缩小版全栈持续运行，灾难时放大 |
| Multi-site Active/Active | 最高 | 近实时 | 多区域同时服务，Route 53/GA 分流 |

关键词：成本最低 → Backup；低 RPO 且关系型全球灾备 → Aurora Global Database；几乎无中断 → Active/Active。

---

## 10. 成本优化总表

| 资源 | 成本动作 |
|---|---|
| EC2 | Savings Plans/RI、Spot、合适实例族、ASG 缩容 |
| S3 | Lifecycle、Intelligent-Tiering、压缩、删除无用版本 |
| EBS | gp2→gp3、删除未挂载卷、快照生命周期 |
| RDS | RI、Aurora Serverless、读副本按需、停开发库 |
| DynamoDB | On-demand vs Provisioned 选择，TTL，避免扫描 |
| 数据传输 | CloudFront 缓存、避免跨 AZ/跨区域不必要流量 |
| 监控日志 | Log retention，过滤高量日志 |

SAA 中“cost-effective”不是永远选最便宜，而是在满足安全/可用/性能约束下成本最低。

---

## 11. Well-Architected 六大支柱

- **Operational Excellence**：IaC、可观测性、自动化变更、小步发布。
- **Security**：最小权限、加密、审计、自动安全检测。
- **Reliability**：多 AZ、自动恢复、备份、配额管理、灾备演练。
- **Performance Efficiency**：托管服务、缓存、弹性扩展、正确存储/数据库选择。
- **Cost Optimization**：按需付费、停止浪费、计费模型、生命周期。
- **Sustainability**：提高利用率、托管服务、合适区域和实例。

考试中它常以“哪种设计更符合最佳实践”出现：优先托管、自动化、最小权限、多 AZ、按需扩展。
